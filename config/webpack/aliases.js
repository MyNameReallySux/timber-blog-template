const paths = require('../paths')

module.exports = {
    '@app': paths.app,
	'@client': paths.client,
	'@config': paths.config,	
	'@libs': paths.libs,
	'@modules': paths.node_modules,
    '@server': paths.server,
    '@theme': paths.theme,    
    '@wp': paths.wp
}